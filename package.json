{
  "name": "micromachines",
  "private": true,
  "version": "0.0.0",
  "license": "Apache-2.0",
  "engines": { "node": ">=20", "pnpm": ">=9" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
